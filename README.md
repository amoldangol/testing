# This is a readme file.

## creating a branch
creates a develop branch and check it out
```shell
git checkout -b develop
```

## view branch
the branch with * denotes current active branch
view the current active branch
``shell
git branch
``

## merge commits
navigate to the destination branch, if you want to merge to
main branch move to main branch first
``shell
git merge <soure-branch>

## Contributor
Amol 